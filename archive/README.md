# Archive Policy

The upstream main branch and Git history are the primary recovery source for removed upstream files. Do not duplicate the entire upstream tree here.

Use this directory only for a confirmed legacy artifact that must remain visible outside normal Git history. Each archived item must record its source version, reason, date and replacement.

Delete an archived item only after confirming that its history and replacement are recoverable.
