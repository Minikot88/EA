# Kurama v0.1.0 Parameters

No parameter set is promoted for this baseline yet.

Store every set file for v0.1.0 in this directory. The file must identify broker, account type, symbol, timeframe and a unique profile or test ID.

Parameter experiments may change values only. If source/Kurama.mq5 changes, create a new EA version and a new parameter directory.
