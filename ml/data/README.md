# ML data

Raw ticks and derived datasets are local-only and ignored by Git.

Committed evidence is limited to manifests, hashes, schemas and data-gate receipts. Training is forbidden until the gate in `DATA_GATE.json` reports `ready`.
