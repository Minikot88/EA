# Kurama v0.1.0 Release

Kurama_v0.1.0.mq5 is the immutable source snapshot for Git tag v0.1.0.

The snapshot must remain byte-identical to src/Kurama.mq5 at the tagged commit. SHA256SUMS records its digest.

This baseline is rename-only and is not a claim of live-trading readiness or multi-broker qualification.
